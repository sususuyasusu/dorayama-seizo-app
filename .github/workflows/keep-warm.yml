name: keep-warm

on:
  schedule:
    - cron: '*/5 * * * *'
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    timeout-minutes: 2
    steps:
      - name: Ping app
        run: |
          for i in 1 2 3; do
            code=$(curl -s -o /dev/null -w "%{http_code}" --max-time 90 https://dorayama-seizo-app-1.onrender.com/ || echo timeout)
            echo "attempt=$i code=$code"
            [ "$code" = "200" ] && exit 0
            sleep 15
          done
          exit 1
